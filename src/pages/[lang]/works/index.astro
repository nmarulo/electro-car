---
import Layout from '../../../layouts/Layout.astro';
import FinishedWorks from '../../../components/works/FinishedWorks.astro';
import {languages} from '../../../i18n/ui';

export function getStaticPaths() {
  return Object.keys(languages)
               .map(lang => ({
                 params: {
                   lang
                 }
               }));
}
---

<Layout>
  <FinishedWorks/>
</Layout>
